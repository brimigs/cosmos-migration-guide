#!/usr/bin/env bash

set -euo pipefail

if [[ "${1:-}" == +* ]]; then
  toolchain="${1#+}"
  shift
else
  toolchain="${RUSTUP_HOST_TOOLCHAIN:-$(rustup show active-toolchain | awk '{ print $1 }')}"
fi

exec rustup run "$toolchain" cargo "$@"
